// Firebase Messaging is disabled until GoogleService-Info.plist / google-services.json
// are configured. Re-enable by restoring the full implementation and adding
// '@react-native-firebase/app' + '@react-native-firebase/crashlytics' to app.config.ts plugins.

export const useFCM = () => {
  // no-op
};
