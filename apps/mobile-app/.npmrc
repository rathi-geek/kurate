# Force a flat node_modules structure for Metro compatibility
node-linker=hoisted
shamefully-hoist=true

# Save exact versions (matches your preference)
save-exact=true

# Handle peer dependencies automatically
strict-peer-dependencies=false
auto-install-peers=true