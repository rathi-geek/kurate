/**
 * Kurate — Framer Motion presets (SOUL.md).
 *
 * Re-exports from the central @/lib/motion-variants.
 * Kept for backward compatibility — existing component imports continue to work.
 * Prefer importing directly from "@/lib/motion-variants" in new code.
 */
export {
  springSnappy,
  springGentle,
  springBouncy,
  springHeavy,
  springTab,
  tabVariants,
  pageVariants,
  fadeUp,
  fadeUpHero,
  fadeIn,
  slideUpSmall,
  fadeUpRight,
  fadeUpLeft,
  slideInLeft,
  slideInRight,
  messageVariants,
  staggerContainer,
  staggerContainerSlow,
  staggerItem,
  cardStagger,
  tapScale,
  hoverLift,
  hoverLiftSlight,
  hoverScale,
  spinnerTransition,
  cssAnimations,
  shadowFloating,
  shadowHoverGlow,
  successGlowBoxShadow,
  shadowResting,
  successGlowTransition,
} from "@/lib/motion-variants";
