// @kurate/query — TanStack Query client, keys, and provider shared across web and mobile
// Seed: copy apps/web/src/app/_libs/query/ → libs/query/src/
// Then re-export from here, e.g.:
export { getQueryClient } from "./client";
export { queryKeys } from "./keys";
export { QueryProvider } from "./QueryProvider";
